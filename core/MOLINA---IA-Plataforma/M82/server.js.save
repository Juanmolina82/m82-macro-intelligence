const express = require('express'); // O el framework que uses
const app = express();

// ... el resto de tu código ...

app.listen(3000, '0.0.0.0', () => {
  console.log('Servidor listo en el puerto 3000');
});
0

